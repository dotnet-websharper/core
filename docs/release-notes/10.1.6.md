This is a minor release of WebSharper 10.1. NuGet version `10.1.6`.

Templates for .NET Core SDK: `dotnet new install WebSharper.Templates@10.1.6`

## Enhancements
* #1547 F# anonymous records are now resolved from FCS anonymous-record details instead of loading the compiled assembly with Reflection.

## Fixes
